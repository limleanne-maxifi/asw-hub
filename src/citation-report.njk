---
layout: base.njk
title: AI Citation Tracker
description: Track whether the ASW Hub is cited by Claude, ChatGPT, Perplexity, Google AI Overviews, Microsoft Copilot, and Gemini when answering the standard ATM query set Maxifi Digital monitors monthly.
permalink: /citation-report/
eleventyExcludeFromCollections: true
---

{% set nextDays = citations.summary.firstFullAudit | daysUntil %}

<header class="page-header">
  <div class="container">
    <p class="eyebrow eyebrow-monitoring">
      <span class="live-dot" aria-hidden="true"></span>
      <span>Monitoring</span>
      <span class="eyebrow-sep">·</span>
      <span>6 engines · {{ citations.queries.length }} queries · monthly</span>
    </p>
    <h1>AI Citation Tracker</h1>
    <p>Per-engine citation tracking across six AI platforms — Claude, ChatGPT, Perplexity, Google AI Overviews, Microsoft Copilot, and Gemini. A standard set of ATM operator queries is submitted to each engine monthly, targeting the hub's citable insight pages and the buyer discovery journey. A citation is recorded only when an engine links to or quotes a hub page.</p>

    <dl class="audit-strip" aria-label="Audit schedule">
      <div>
        <dt>Last audit</dt>
        <dd><time datetime="{{ citations.summary.lastUpdated }}">{{ citations.summary.lastUpdated | longDate }}</time></dd>
      </div>
      <div>
        <dt>Next full audit</dt>
        <dd>
          <time datetime="{{ citations.summary.firstFullAudit }}">{{ citations.summary.firstFullAudit | longDate }}</time>
          {% if nextDays > 0 %}<span class="audit-countdown">in {{ nextDays }} day{% if nextDays != 1 %}s{% endif %}</span>{% elif nextDays == 0 %}<span class="audit-countdown">today</span>{% endif %}
        </dd>
      </div>
      <div>
        <dt>Indexing window</dt>
        <dd>First engines cited July 2026 · full six-engine audit completes later this month</dd>
      </div>
    </dl>
  </div>
</header>

<section>
  <div class="container">

    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1.5rem; margin-bottom: 3rem;">
      <div style="background: #f5f5f5; padding: 1.5rem; border-radius: 8px;">
        <p style="margin: 0; font-size: 0.875rem; color: #555; font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em;">Queries tracked</p>
        <p style="margin: 0.5rem 0 0 0; font-size: 2.5rem; font-weight: 700; color: #111;">{{ citations.queries.length }}</p>
        <p style="margin: 0.5rem 0 0 0; font-size: 0.875rem; color: #777;">Standard test queries</p>
      </div>
      <div style="background: #f5f5f5; padding: 1.5rem; border-radius: 8px;">
        <p style="margin: 0; font-size: 0.875rem; color: #555; font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em;">Engines monitored</p>
        <p style="margin: 0.5rem 0 0 0; font-size: 2.5rem; font-weight: 700; color: #111;">{{ citations.engines.length }}</p>
        <p style="margin: 0.5rem 0 0 0; font-size: 0.875rem; color: #777;">{% for e in citations.engines %}{{ e.name }}{% if not loop.last %} · {% endif %}{% endfor %}</p>
      </div>
      <div style="background: #f5f5f5; padding: 1.5rem; border-radius: 8px;">
        <p style="margin: 0; font-size: 0.875rem; color: #555; font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em;">Citations recorded</p>
        <p style="margin: 0.5rem 0 0 0; font-size: 2.5rem; font-weight: 700; color: #111;">{{ citations.summary.totalCitations }}</p>
        <p style="margin: 0.5rem 0 0 0; font-size: 0.875rem; color: #777;">First audit · 2 July 2026</p>
      </div>
    </div>

    <div class="context-strip">
      <aside class="context-card context-card--window">
        <p class="context-label">Indexing window</p>
        <p class="context-body">A new hub takes 4–8 weeks for AI engines to crawl, index, and start citing. Fast movers (Perplexity, Claude) show first; Google AI Overviews, Copilot, and Gemini lag.</p>
      </aside>

      {% if citations.testCase %}
      <aside class="context-card context-card--testcase">
        <p class="context-label">{{ citations.testCase.label }}</p>
        <p class="context-link"><a href="{{ citations.testCase.targetUrl }}">{{ citations.testCase.targetTitle }} →</a></p>
        <p class="context-body">The hub's first recorded AI citation: Microsoft Copilot's Bing AI answer lists this insight page as a source, drawing on its framing of converging regulatory cycles and the AI inflection point (2 July 2026).</p>
      </aside>
      {% endif %}
    </div>

    <h2 style="margin-bottom: 0.5rem;">Citation status by standard query</h2>
    <p style="color: var(--paper-faint); margin-bottom: 2rem; font-size: 0.9rem;">Each cell shows whether that AI engine cited the hub when answering this query in the latest audit. Green = cited; grey = not yet measured. The <strong>Target</strong> column shows the page each query is designed to surface.</p>

    <div style="overflow-x: auto;">
      <table style="width: 100%; border-collapse: collapse; font-size: 0.875rem;">
        <thead>
          <tr>
            <th style="text-align: left; padding: 0.75rem 1rem; background: #f5f5f5; border-bottom: 2px solid #e0e0e0; color: #111;">Standard query</th>
            <th style="text-align: left; padding: 0.75rem 0.75rem; background: #f5f5f5; border-bottom: 2px solid #e0e0e0; white-space: nowrap; font-size: 0.75rem; color: #111;">Tier</th>
            <th style="text-align: left; padding: 0.75rem 0.75rem; background: #f5f5f5; border-bottom: 2px solid #e0e0e0; white-space: nowrap; font-size: 0.75rem; color: #111;">Target page</th>
            {% for engine in citations.engines %}
            <th style="text-align: center; padding: 0.75rem 0.75rem; background: #f5f5f5; border-bottom: 2px solid #e0e0e0; white-space: nowrap; font-size: 0.75rem; color: #111;">{{ engine.name }}</th>
            {% endfor %}
          </tr>
        </thead>
        <tbody>
          {% for query in citations.queries %}
          <tr style="border-bottom: 1px solid var(--hairline);">
            <td style="padding: 1rem; font-style: italic; color: var(--paper-mute); max-width: 340px;">{{ query.text }}</td>
            <td style="padding: 1rem 0.75rem; font-size: 0.75rem; color: var(--paper-mute); white-space: nowrap;">
              {% if query.tier == "Control" %}
                <span style="display: inline-block; padding: 2px 8px; background: #e5e7eb; color: #374151; border-radius: 999px; font-weight: 600; letter-spacing: 0.04em;">Control</span>
              {% else %}
                <span style="display: inline-block; padding: 2px 8px; background: #dbeafe; color: #1d4ed8; border-radius: 999px; font-weight: 600; letter-spacing: 0.04em;">{{ query.tier }}</span>
              {% endif %}
            </td>
            <td style="padding: 1rem 0.75rem; font-size: 0.8rem;">
              {% if query.targetUrl %}<a href="{{ query.targetUrl }}" style="color: var(--paper-mute); text-decoration: underline;">{{ query.targetLabel or query.targetUrl }}</a>{% else %}<span style="color: var(--paper-faint);">—</span>{% endif %}
            </td>
            {% for engine in citations.engines %}
            {% set engineData = query.engines[engine.id] %}
            <td style="text-align: center; padding: 1rem 0.75rem;">
              {% if engineData and engineData.cited %}
                {% set citedTitle = engineData.quote or ("Hub cited by " + engine.name) %}
                {% if engineData.url %}
                <a href="{{ engineData.url }}" target="_blank" rel="noopener" title="{{ citedTitle }}" aria-label="Cited by {{ engine.name }}" style="display: inline-block; width: 1.5rem; height: 1.5rem; background: #16a34a; border-radius: 50%; text-align: center; line-height: 1.5rem; color: white; font-size: 0.7rem; text-decoration: none;">✓</a>
                {% else %}
                <span title="{{ citedTitle }}" aria-label="Cited by {{ engine.name }}" style="display: inline-block; width: 1.5rem; height: 1.5rem; background: #16a34a; border-radius: 50%; text-align: center; line-height: 1.5rem; color: white; font-size: 0.7rem;">✓</span>
                {% endif %}
              {% else %}
                <span title="Not yet measured — {{ engine.name }}" aria-label="Not yet measured" style="display: inline-block; width: 1.5rem; height: 1.5rem; border: 2px solid var(--paper-faint); border-radius: 50%; line-height: 1.3rem; text-align: center; color: var(--paper-faint); font-size: 0.85rem;">–</span>
              {% endif %}
            </td>
            {% endfor %}
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

    <p style="margin-top: 1rem; font-size: 0.8rem; color: var(--paper-faint);">✓ = hub cited in latest audit &nbsp;·&nbsp; – = not yet measured &nbsp;·&nbsp; Click ✓ to see the source response</p>

    <aside style="margin-top: 2.5rem; padding: 1.5rem 1.75rem; background: #f5f5f5; border-left: 3px solid #16a34a; border-radius: 8px;">
      <p style="margin: 0 0 0.75rem 0; font-size: 0.8rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: #111;">First audit — July 2026</p>
      <p style="margin: 0 0 0.75rem 0; font-size: 0.9rem; color: #333; line-height: 1.65;">The first full audit ran on 2 July 2026 across Claude, Perplexity and Microsoft Copilot; ChatGPT, Gemini and Google AI Overviews complete later in July. The hub's first tracked insight citation landed on Microsoft Copilot, whose Bing AI answer to "Why does Airspace World 2026 matter for the air traffic management industry?" lists the <a href="/insights/why-asw-2026-matters/" style="color: #1d4ed8;">Why ASW 2026 matters</a> insight page as a source.</p>
      <p style="margin: 0 0 0.75rem 0; font-size: 0.9rem; color: #333; line-height: 1.65;">Beyond the tracked insights, the hub won the buyer discovery query outright on two engines. For <em>"Where can I find session summaries and speaker insights from Airspace World 2026?"</em>, Microsoft Copilot's AI answer is built from hub pages — the hub is the #1 Bing result, above airspaceworld.com — and Perplexity cites the hub as "the best starting point" for the same query.</p>
      <p style="margin: 0; font-size: 0.85rem; color: #555; line-height: 1.65;">Methodology unchanged: the standard ATM operator query set is submitted manually to each engine, and a citation is recorded only as a direct link or a verbatim quote.</p>
    </aside>

  </div>
</section>

<section>
  <div class="container">
    <div class="section-head">
      <p class="eyebrow">FAQ</p>
      <h2>Frequently asked questions about the tracker</h2>
    </div>

    <div class="prose" style="margin-top: 1.5rem;">
      <h3>What does the AI Citation Tracker measure?</h3>
      <p>It tracks whether the ASW Hub is cited by six AI engines — Claude, ChatGPT, Perplexity, Google AI Overviews, Microsoft Copilot, and Gemini — when answering a standard set of air traffic management queries that Maxifi Digital submits to each engine monthly. The queries target the hub's citable insight pages and the buyer discovery journey, testing whether structured hub content is picked up in AI answers.</p>

      <h3>Which AI engines are monitored?</h3>
      <p>Six: Claude (Anthropic), ChatGPT (OpenAI), Perplexity, Google AI Overviews, Microsoft Copilot, and Gemini (Google DeepMind). Each engine is checked against the same standard query set.</p>

      <h3>How often is the tracker updated?</h3>
      <p>Monthly. The standard query set is submitted manually to all six engines each month, and the results table updates after each run. The first full audit ran on 2 July 2026 across Claude, Perplexity, and Microsoft Copilot; ChatGPT, Gemini, and Google AI Overviews complete later in July.</p>

      <h3>How is a citation recorded?</h3>
      <p>A citation is recorded when an engine either links directly to a page on this hub or quotes its content verbatim in the answer. Until that happens for a given engine and query, the cell is marked as not yet measured.</p>

      <h3>How long does it take for a new hub to be cited by AI engines?</h3>
      <p>A new hub typically takes four to eight weeks for AI engines to crawl, index, and start citing. Fast movers such as Perplexity and Claude tend to show first; Google AI Overviews, Microsoft Copilot, and Gemini usually lag.</p>

      <h3>Which pages does the tracker follow?</h3>
      <p>The hub's citable insight pages — including "Why ASW 2026 matters more than the last three combined" and "Why FAQs beat blogs for AI citation" — alongside buyer discovery queries such as where to find Airspace World 2026 session summaries and speaker insights. In the July 2026 audit, Microsoft Copilot cited the "Why ASW 2026 matters" page, and both Copilot and Perplexity surfaced the hub for the discovery query.</p>
    </div>
  </div>
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "What does the AI Citation Tracker measure?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "It tracks whether the ASW Hub is cited by six AI engines — Claude, ChatGPT, Perplexity, Google AI Overviews, Microsoft Copilot, and Gemini — when answering a standard set of air traffic management queries that Maxifi Digital submits to each engine monthly. The queries target the hub's citable insight pages and the buyer discovery journey, testing whether structured hub content is picked up in AI answers."
      }
    },
    {
      "@type": "Question",
      "name": "Which AI engines are monitored?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Six: Claude (Anthropic), ChatGPT (OpenAI), Perplexity, Google AI Overviews, Microsoft Copilot, and Gemini (Google DeepMind). Each engine is checked against the same standard query set."
      }
    },
    {
      "@type": "Question",
      "name": "How often is the tracker updated?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Monthly. The standard query set is submitted manually to all six engines each month, and the results table updates after each run. The first full audit ran on 2 July 2026 across Claude, Perplexity, and Microsoft Copilot; ChatGPT, Gemini, and Google AI Overviews complete later in July."
      }
    },
    {
      "@type": "Question",
      "name": "How is a citation recorded?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "A citation is recorded when an engine either links directly to a page on this hub or quotes its content verbatim in the answer. Until that happens for a given engine and query, the cell is marked as not yet measured."
      }
    },
    {
      "@type": "Question",
      "name": "How long does it take for a new hub to be cited by AI engines?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "A new hub typically takes four to eight weeks for AI engines to crawl, index, and start citing. Fast movers such as Perplexity and Claude tend to show first; Google AI Overviews, Microsoft Copilot, and Gemini usually lag."
      }
    },
    {
      "@type": "Question",
      "name": "Which pages does the tracker follow?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "The hub's citable insight pages — including \"Why ASW 2026 matters more than the last three combined\" and \"Why FAQs beat blogs for AI citation\" — alongside buyer discovery queries such as where to find Airspace World 2026 session summaries and speaker insights. In the July 2026 audit, Microsoft Copilot cited the \"Why ASW 2026 matters\" page, and both Copilot and Perplexity surfaced the hub for the discovery query."
      }
    }
  ]
}
</script>

<section class="section-tight" style="border-bottom: 0;">
  <div class="container">
    <div class="maxifi-cta">
      <div>
        <p class="eyebrow">Methodology</p>
        <h2>How citations are tracked</h2>
        <p>Each month, the standard ATM operator query set below is submitted manually to all six engines. The queries mix <strong>buyer discovery</strong> — head-of-funnel browse queries for the whole event — with <strong>insight-targeted queries</strong> written as a real attendee, journalist, or operator would phrase them, each pointed at a citable hub page. A citation is recorded when an engine either links directly to a page on this hub or quotes its content verbatim in the answer. Results update here after each monthly run. To update: set <code>cited: true</code> and add the source <code>url</code> in <code>src/_data/citations.json</code>, then rebuild.</p>
        <ol style="color: var(--paper-mute); padding-left: 1.4em; margin: 1.25em 0 0; line-height: 1.8;">
          {% for query in citations.queries %}
          <li style="font-size: 0.9rem;">{{ query.text }}</li>
          {% endfor %}
        </ol>
      </div>
      <div class="maxifi-cta-actions">
        <a class="btn btn-primary" href="/insights/">All insights →</a>
        <a class="btn btn-ghost" href="/how-it-works/">See the methodology</a>
      </div>
    </div>
  </div>
</section>
