{
  "name": "asw-hub",
  "version": "1.0.0",
  "description": "Airspace World 2026 AEO Hub — the canonical ATM resource for Airspace World 2026.",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rimraf _site",
    "debug": "DEBUG=Eleventy* eleventy",
    "citation-check": "node scripts/citation-check.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@anthropic-ai/sdk": "^0.95.1",
    "@google/genai": "^1.0.0",
    "openai": "^6.0.0",
    "rimraf": "^6.1.3"
  }
}
